"""
The `src` package contains modules that implement the Flask web application and
command-line interface for the Meme Generator.

Modules:
- app.py: Implements the Flask web application and provides routes for
generating memes via web forms and HTTP requests.
- meme.py: Implements the command-line interface for generating memes.
- templates/: Contains the HTML templates used by the Flask application for
rendering web pages.

Usage:
To run the web application, navigate to the `src/` directory and run `python
app.py`. This will start the Flask development server and make the web
application available at `http://localhost:5000/`.

To use the command-line interface, navigate to the `src/` directory and run
`python meme.py`. This will display usage information for the command-line
interface and the available options.
"""
